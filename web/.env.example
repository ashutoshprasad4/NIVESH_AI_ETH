# ============================================================
# AI Trading Copilot — Environment Variables Template
# Copy this file to .env.local and fill in your values.
# NEVER commit .env.local or .env to git.
# ============================================================

# --- AI Keys (used in the Settings panel in the app) ---
# At least one is required for the Copilot chat to work.
NEXT_PUBLIC_GEMINI_API_KEY=your_gemini_api_key_here
NEXT_PUBLIC_OPENAI_API_KEY=your_openai_api_key_here

# --- Optional: default AI model ---
# Options: gemini-2.0-flash, gemini-1.5-flash, gpt-4o-mini
NEXT_PUBLIC_DEFAULT_MODEL=gemini-2.0-flash
